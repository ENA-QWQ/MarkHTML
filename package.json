{
  "name": "markhtml",
  "version": "1.0.0",
  "description": "Online Markdown to HTML converter with themes and syntax highlighting",
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
